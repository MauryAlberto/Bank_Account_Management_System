#include "Bank.hpp"

int main(){
    Bank bank;
    /* bank.loadAllAccounts(); */

    int choice;
    do{
        // Display menu
        // Call corresponding bank method
    }while(choice != 0);

    /* bank.saveAllAccounts(); */

    return 0;
}